@page
@model ContactTracing.Views.Students.ClassroomAssignments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClassroomAssignment[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassroomAssignment[0].Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassroomAssignment[0].Classroom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ClassroomAssignment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Classroom.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
